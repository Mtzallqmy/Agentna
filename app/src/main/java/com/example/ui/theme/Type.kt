package com.mtzallqmy.agentna.ui.theme

import androidx.compose.material3.Typography

/**
 * Material 3 defaults deliberately keep Arabic and Latin fallback behavior correct
 * across Android releases. Visual identity is applied through color, spacing and shape
 * rather than bundling a font asset into the app.
 */
val Typography = Typography()
